package com.example.moodflow.theme

import androidx.compose.ui.unit.dp

// ── Border Radius ──
val RadiusXl = 34.dp
val RadiusLg = 26.dp
val RadiusMd = 18.dp
val RadiusFull = 999.dp

// ── Screen Padding ──
val ScreenPaddingHorizontal = 28.dp
val ScreenPaddingTop = 34.dp

// ── Component Sizes ──
val BottomNavHeight = 72.dp
val PrimaryButtonHeight = 62.dp
val ChipHeight = 48.dp
val IconButtonSize = 38.dp
val StatusBarHeight = 44.dp

// ── Orb Sizes ──
val OrbXlSize = 255.dp
val OrbHeroWidth = 234.dp
val OrbHeroHeight = 222.dp
val OrbCanvasWidth = 250.dp
val OrbCanvasHeight = 300.dp
val OrbSmallSize = 78.dp

// ── Spacing ──
val SpacingXs = 4.dp
val SpacingSm = 8.dp
val SpacingMd = 12.dp
val SpacingLg = 16.dp
val SpacingXl = 20.dp
val SpacingXxl = 24.dp
val SpacingXxxl = 28.dp
val Spacing32 = 32.dp
val Spacing34 = 34.dp
val Spacing40 = 40.dp

// ── Dot Sizes ──
val MoodDotSize = 36.dp
val MoodDotLargeSize = 52.dp
val InsightDotSize = 44.dp

// ── Card ──
val MemoryCardMinHeight = 128.dp
val MemoryCardCompactMinHeight = 114.dp
val ObservationRowMinHeight = 94.dp

// ── Tag ──
val TagMinHeight = 24.dp

// ── Pager dots ──
val PagerDotSize = 7.dp
val PagerDotActiveWidth = 17.dp

// ── Progress line ──
val ProgressLineHeight = 2.dp

// ── Close / back button ──
val CloseButtonSize = 36.dp

// ── App icon ──
val AppIconSize = 42.dp
val AppIconRadius = 14.dp

// ── Filter tab ──
val FilterTabHeight = 34.dp

// ── Energy option button ──
val EnergyButtonHeight = 48.dp

// ── Textarea ──
val TextareaHeight = 112.dp
val TextareaRadius = 24.dp

// ── Check circle ──
val CheckCircleSize = 46.dp

// ── Save button ──
val SaveButtonHeight = 62.dp
